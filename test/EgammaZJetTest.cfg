# CSA07 Skim for Z(->EE/MuMu)+Jet(->SuperCluster) Events
# Filter events with two HLT electrons/muons isolated via trigger bit
# plus one SuperCluster or a RecoJet (note that we require 3 SC to account for those 
# in the HLT electrons)
# Created by Alexey Atramentov, Roberto Salerno

process EgammaSkim= {

    ## Paths and Modules
    include "EgammaAnalysis/CSA07Skims/data/EgammaZJetSkimPath.cff"
    include "EgammaAnalysis/CSA07Skims/data/EgammaZJetOutputModule.cfi"

    ### keep the logging output to a nice level ###
    include "FWCore/MessageLogger/data/MessageLogger.cfi"

    service = AdaptorConfig {}

    untracked PSet maxEvents = {
        untracked int32 input = 10
    }
    
    source = PoolSource {
	untracked vstring fileNames =
	{
         "rfio:/castor/cern.ch/user/m/mmozer/z_ee.root"
	}
	untracked uint32 debugVerbosity = 0
	untracked bool   debugFlag     = false
    }

   module evtInfo = AsciiOutputModule {
   }

   endpath end = { evtInfo, AODSIMZJetEventContent, RECOSIMZJetEventContent }
}
